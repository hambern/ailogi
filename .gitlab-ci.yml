stages:
  - debug

debug:
  stage: debug
  before_script:
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keygen -y -f ~/.ssh/id_rsa
  script:
    - echo "Privat nyckel fungerar och är korrekt!"
  only:
    - main
